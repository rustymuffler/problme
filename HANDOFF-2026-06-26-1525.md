# Handoff — 2026-06-26 15:25

**Session type:** Code session — Developer Agent + PM Agent
**Build status:** Passes — all CI green, main is clean, all PRs merged

---

## What Was Accomplished This Session

### PR #42 — Dependabot vulnerabilities resolved (MERGED)
- Added npm `overrides` to `package.json` to pin three vulnerable transitive deps in `@lhci/cli@0.15.1` (latest — no upstream fix available):
  - `js-yaml` 3.14.2 → 4.2.0 (Dependabot #70, medium)
  - `tmp` 0.0.33 → 0.2.7 (Dependabot #69/#66, high/low)
  - `uuid` 8.3.2 → 11.1.1 (Dependabot #68, medium)
- Remaining audit noise: `yaml` via `@astrojs/check` (Dependabot #67, auto-dismissed by GitHub — only fix is a breaking downgrade)
- Aikido scan: 0 findings. Build: passes.

### PR #43 — CHANGELOG.md v0.1.0 entry (MERGED)
- Replaced the stub in `CHANGELOG.md` with a full v0.1.0 entry covering Phase 1 infrastructure, Phase 2 design system, content pipeline, published articles, security fixes
- Ticked M1.17 ✅ in MILESTONES.md
- **Phase 1 is now 100% complete** — all 18 milestones done

### PR #44 — Google Search Console verification (MERGED)
- Added `<meta name="google-site-verification" content="jnYt7upARHlPd6_-vw_aWNOy5obt6s7p0DXAPPnal6M">` to `Layout.astro`
- Richard verified ownership in Search Console post-deploy ✅
- Richard submitted `sitemap-index.xml` to Search Console ✅

### PR #45 — M3.16 milestone tick (MERGED)
- MILESTONES.md: M3.16 marked ✅

---

## Current State of Main

- **Phase 1:** ✅ Complete — all 18 items done
- **Phase 2:** ✅ Complete — all 19 items done
- **Phase 3:** 🔄 In progress — M3.16 done, M3.17–M3.24 (Umami analytics) not started

---

## Active Blockers

| # | Blocker | Status |
|---|---|---|
| 5 | Supabase account | ⬜ Not started — blocks Umami analytics (M3.17) |
| 6 | Vercel account | ⬜ Not started — blocks Umami hosting (M3.19) |
| 7 | Umami data-website-id | 🔑 Waiting on M3.21 |

---

## Files Created or Modified This Session (all merged to main)

| File | What happened |
|---|---|
| `package.json` | Added `overrides` block for js-yaml, tmp, uuid |
| `package-lock.json` | Updated by npm install after overrides |
| `CHANGELOG.md` | Full v0.1.0 entry replacing the stub |
| `MILESTONES.md` | M1.17 and M3.16 marked ✅ |
| `src/layouts/Layout.astro` | Added Google Search Console verification meta tag |

---

## Critical Invariants (Never Violate)

- **No em dashes** anywhere in article content or UI copy. Ever.
- **Interview notes are required** before any article is drafted. No exceptions.
- **Proofreader must approve** before Publisher Agent opens a PR.
- **Hero images** go in `public/assets/posts/[slug]/` and use absolute paths in frontmatter.
- **Pipeline docs** (interview notes, research briefs) go in `content-pipeline/[slug]/`, not `src/content/posts/`.
- **Astro has no date-gated publishing.** Scheduling = merge the PR on the publish date.
- **rl-protect scan before every npm install.** No exceptions.
- **Dark mode only.** Background `#15120E`.
- **Fonts:** Hanken Grotesk + JetBrains Mono only.
- **No secrets in source files.**
- **Lighthouse minimums:** Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95.

---

## Key Decisions Made This Session

1. **Dependabot #67 (`yaml` via `@astrojs/check`) left unfixed.** GitHub auto-dismissed it. The only available fix requires downgrading `@astrojs/check` to a breaking older version. Accepted risk — logged in this handoff.
2. **Google Search Console verified via HTML tag method.** Tag lives in `Layout.astro` and ships on every page.

---

## Exact Next Steps

### What Richard must do first (Claude is blocked until these are done)

**Supabase (M3.17):**
1. Go to [supabase.com](https://supabase.com) → create a free account
2. New project — name it `umami`, pick a region, set a strong database password
3. Wait ~2 min for provisioning
4. Project Settings → Database → Connection string tab → copy two strings:
   - **Transaction pooler** (port 6543) → this is `DATABASE_URL`
   - **Direct connection** (port 5432) → this is `DIRECT_DATABASE_URL`
5. Paste both strings to Claude in the new session

**Vercel (M3.19):**
1. Go to [vercel.com](https://vercel.com) → Continue with GitHub
2. Authorize the GitHub integration when prompted
3. Tell Claude "Vercel is ready" — do not import any repo yet

Both can be done in parallel. Neither blocks the other.

### What Claude does once Richard provides credentials

1. Fork `umami-software/umami` to `rustymuffler/umami` (M3.18)
2. Modify `schema.prisma` to wire up `DIRECT_DATABASE_URL` for Prisma migrations (M3.18)
3. Guide Richard through importing the fork into Vercel and setting env vars (M3.20)
4. Guide Richard through deploying Umami and adding probl.me as a tracked site (M3.21)
5. Add Umami tracking script to `Layout.astro` — PR for Richard to merge (M3.22)
6. Verify analytics tracking works (M3.23)
7. Queue C2 article about the Umami setup through the full content pipeline (M3.24)

### Next content session (whenever Richard is ready)

- C1 is next: *"Writing a spec your AI agent can actually follow"* (pm-craft)
- Run Interview Agent first — no drafting without interview notes
- Full pipeline: Interview → Research → Writer → SEO Review → Proofread → Image → Publisher
