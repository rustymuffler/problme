# Handoff — 2026-07-15 01:40

**Session type:** Mixed — content session (C7 published end-to-end) + code session (10 supporting infra/docs PRs)
**Build status:** Passes — all CI green, `main` is clean, all PRs merged, article confirmed live

---

## What Was Accomplished This Session

### PR #55 — C7 published: "Why I SHA-Pin Every GitHub Action (and Automate Updates)" (MERGED)
Full pipeline run end-to-end for the first time (M3.7): interview → research → draft → hero + in-article images (hand-built SVG, rasterized via `sharp`, no image-gen tool was available) → SEO review → proofread → PR. Live at `probl.me/blog/pin-github-actions-dependabot`, confirmed HTTP 200. Included a required fix to `src/content.config.ts` (glob pattern was matching every `.md` file in `src/content/posts/`, including `interview-notes.md`/`research-brief.md`, breaking the build; narrowed to `**/index.{md,mdx}`).

### PR #48, #54 — Two real gaps found while researching C7 (MERGED)
- **#48:** `.github/workflows/scorecard.yml` didn't exist and `SECURITY_SCANNING.md` had no automated Dependabot config. Added `.github/dependabot.yml` for `github-actions`.
- **#54:** `SECURITY_SCANNING.md` falsely claimed Checkov enforces SHA-pinning of Actions. It doesn't (verified: `CKV_GHA_1`–`CKV_GHA_7`, `CKV2_GHA_1` cover unrelated checks). Added OpenSSF Scorecard (`scorecard.yml`) as the real tool for this, kept `publish_results: false` — see Decision 10 in `DECISIONS.md`.

### PR #49–#53 — Dependabot's first automated bumps (MERGED)
Proof the #48 config works: `actions/setup-node`, `actions/configure-pages`, `actions/checkout` (major bump v4→v7), `actions/upload-pages-artifact`, `lycheeverse/lychee-action` all bumped automatically.

### PR #58 — Lychee config fix + missing resume PDF (MERGED)
PR #53's lychee bump failed CI: newer lychee (0.24.2) renamed `exclude_mail` → `include_mail`. Fixing that surfaced a bigger issue: without `root_dir` set, lychee was silently skipping ~500 internal links (33 checked on `main` vs 526 once fixed). That in turn exposed a genuinely broken resume download link live on `probl.me/about`. Richard supplied the real PDF.

### PR #56, #57 — Documentation corrections (MERGED)
`CONTENT_STANDARDS.md`/`AGENTS.md` described a frontmatter schema, image path convention, and callout syntax that didn't match the real implementation (would have broken the build if followed literally). Corrected against the actual `content.config.ts` schema and the one published article. Added `SECURITY.md`.

### PR #59, #61 — Scorecard score tracking (MERGED)
Score improved 6.1 → 6.9 → 7.0 over the session as Security-Policy, Branch-Protection (partial, via a ruleset Richard configured directly), and SAST recognition closed. Full breakdown in `SCORECARD_IMPROVEMENTS.md`.

### PR #60 — Semgrep SARIF upload (MERGED)
`semgrep/semgrep-action@v1` has no SARIF output at all (verified its `action.yml`: only `config`/`publishToken` inputs). Added a second Semgrep invocation (raw CLI, pinned to `semgrep==1.169.0` after a Pinned-Dependencies regression was caught and fixed same-day) purely to produce SARIF for GitHub code scanning.

### PR #62 — Content calendar close-out (MERGED)
C7 marked Published, M3.7 marked complete.

### PR #63 — README badges + diagrams (MERGED)
Added CI/Security/Deploy/Lighthouse/License badges (all verified resolving), Local Development / Security / Built with AI Agents sections, a security-stack diagram, and an animated content-pipeline GIF (hand-generated: 24 SVG frames via a `.cjs` script, rasterized with `sharp`, assembled with `ffmpeg`'s two-pass palette).

### PR #64 — Third-party notices + credits page (MERGED, one follow-up fix)
`THIRD-PARTY-NOTICES.md` was stale (4 Dependabot-bumped SHAs never reflected, 3 new actions missing, `@astrojs/rss`/`@astrojs/sitemap` never listed at all). Also found and fixed a real bug: `credits.astro` renders from that file via hardcoded name allow-lists per bucket, so new entries would have been silently invisible without also updating those lists. One follow-up commit fixed a broken `@astrojs/sitemap` source URL that CI's own link checker caught.

### Repo hygiene (no PR — direct actions, both explicitly approved by Richard)
- Deleted 2 stale branches: `feat/phase-2-completion` (had one orphaned commit, content already present on `main` via another path — nothing lost), `revert-16-master` (zero commits ahead of `main`).
- Richard configured a branch protection ruleset ("Main Enforcement") directly: blocks force-push/deletion, requires PR + 7 status checks.

---

## Current State of Main

- **Phase 1, 2:** ✅ Complete (unchanged this session)
- **Phase 3:** 🔄 In progress — M3.7 now ✅ (first full pipeline validation). M3.8–M3.10 (articles 2–4) not started. M3.17–M3.24 (Umami analytics) still not started, blocked on Supabase/Vercel accounts.
- **OpenSSF Scorecard:** 7.0/10, private (`publish_results: false`). See `SCORECARD_IMPROVEMENTS.md` for exactly what's left before considering public.
- **Content queue:** C1, C2, C3, C5, C6 still queued (unchanged). **C8 added this session** — prompt injection article, see below.

---

## Active Blockers

Unchanged from before this session — nothing new hit, nothing resolved:

| # | Blocker | Status |
|---|---|---|
| 5 | Supabase account | ⬜ Not started — blocks Umami analytics (M3.17) |
| 6 | Vercel account | ⬜ Not started — blocks Umami hosting (M3.19) |
| 7 | Umami data-website-id | 🔑 Waiting on M3.21 |

---

## Files Created or Modified This Session

Too many individual files to list exhaustively (10 PRs). By category:

- **Content:** `src/content/posts/pin-github-actions-dependabot/` (index.mdx, interview-notes.md, research-brief.md, images/), `public/assets/posts/pin-github-actions-dependabot/`
- **Workflows:** `.github/workflows/scorecard.yml` (new), `.github/workflows/security.yml`, `.github/dependabot.yml` (new), `.lychee.toml`, 5 Dependabot-bumped SHAs across `ci.yml`/`deploy.yml`/`lighthouse.yml`/`security.yml`
- **Docs:** `SECURITY_SCANNING.md`, `SECURITY.md` (new), `SCORECARD_IMPROVEMENTS.md` (new), `CONTENT_STANDARDS.md`, `AGENTS.md`, `THIRD-PARTY-NOTICES.md`, `MILESTONES.md`, `DECISIONS.md`, `README.md`
- **Code:** `src/content.config.ts` (glob pattern fix), `src/pages/credits.astro` (bucket allow-lists)
- **Assets:** `public/assets/readme/` (content-pipeline.gif + generator script, security-layers.svg/png), `public/assets/Richard-Robitaille-Muffler-Resume.pdf`
- **Dev tooling:** `.claude/launch.json` (new, astro-preview config)

---

## Critical Invariants (Never Violate)

Corrected against verified current reality this session — some values in the previous handoff (2026-06-26) were stale:

- **No em dashes** anywhere in article content or UI copy. Ever.
- **Interview notes and research brief are both required** before any article is drafted. No exceptions.
- **Proofreader must approve** before Publisher Agent opens a PR.
- **Real frontmatter schema** is `title`, `description`, `category`, `tags`, `date`, `readTime`, `heroImage` (optional), `imageCredit` (optional), per `src/content.config.ts`, the actual source of truth. Do NOT use `publishDate`/`slug`/`author`/`imageAlt`/`draft`/`featured`, they don't exist in the schema.
- **Hero + runtime images** go in `public/assets/posts/[slug]/`, absolute paths in frontmatter (`/assets/posts/[slug]/hero.png`). Source design files may additionally live at `src/content/posts/[slug]/images/` for provenance.
- **Interview notes / research briefs live alongside the article**, at `src/content/posts/[slug]/interview-notes.md` and `research-brief.md` — the content collection glob is `**/index.{md,mdx}` specifically so these coexist without being treated as posts. Do not revert that glob pattern.
- **Callouts use `<Callout type="note">...</Callout>`** (import from `../../../components/Callout.astro`), not `:::note` container syntax.
- **Astro has no date-gated publishing.** Scheduling = merge the PR on the publish date.
- **rl-protect scan before every npm install.** No exceptions.
- **Dark mode only.** Background `#0F1117` (per current `BRAND.md` — note the 2026-06-26 handoff said `#15120E`, that value is stale, don't use it).
- **Fonts:** Hanken Grotesk + JetBrains Mono only.
- **No secrets in source files.**
- **Lighthouse minimums:** Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95.
- **All GitHub Actions SHA-pinned**, kept current via Dependabot. When adding a new workflow, verify against Checkov before merging.
- **No live prompt-injection payloads against uncontrolled third parties** — see Decision 11. Inert, clearly-labeled examples only; interactive demos must be explicitly opt-in.
- **`THIRD-PARTY-NOTICES.md` updates must also update `src/pages/credits.astro`'s hardcoded name allow-lists** (`frameworkNames`, `buildNpmNames`, `secBinNames`, `buildActNames`, `secActNames`), or new entries render nowhere.
- **This repo is under OneDrive on Windows with `core.autocrlf=true`.** Locally-built pages that parse markdown with newline-anchored regex can break locally (CRLF) while working fine in CI (Linux, LF). Don't assume a local rendering failure is a production bug without checking `git show HEAD:<file> | file -` first.

---

## Key Decisions Made This Session

Full detail in `DECISIONS.md`:

10. **OpenSSF Scorecard added, results kept private.** Revisit via `SCORECARD_IMPROVEMENTS.md` once remaining actionable gaps (SAST fully recognized, Vulnerabilities triaged) close, not on a fixed date.
11. **Prompt injection article (C8) will not include a live, functioning injection payload against uncontrolled third parties.** Even a harmless payload deployed on the live public page affects real visitors' AI agents without consent. Agreed alternative: opt-in interactive demo + inert example payloads per technique.

---

## Exact Next Steps

### Next content session — C8: Prompt Injection

Richard's pitch (full detail in `MILESTONES.md` Content Calendar C8 note and `DECISIONS.md` #11): he hit a real hidden prompt-injection attempt on lawsofux.com and wants to write about the technique landscape, hidden web pages, DNS TXT records, SEO-poisoned pages, email/calendar-invite/shared-doc payloads, images/PDFs, and untrusted survey/form input as injection vectors. Ties directly into probl.me's own thesis (built and maintained through AI collaboration).

**The live-payload framing is already settled** (Decision 11), don't re-litigate it at the start of the session, just confirm Richard's still on board with the opt-in-demo + inert-examples approach before drafting.

Run the full pipeline:
1. Interview Agent — capture Richard's firsthand lawsofux.com experience and his take on each technique category
2. Research Agent — verify each technique category is real and current (cite sources per category), competitive scan (this is a well-covered security topic, find the differentiated angle, likely "written by a blog that's itself AI-collaborative, walking the walk")
3. Writer Agent → Image Creator → SEO Reviewer → Proofreader → Publisher, as normal

### Other queued articles (unchanged priority): C1, C2, C3, C5, C6

### Still blocked on Richard (unchanged from prior handoffs)

**Supabase (M3.17):** supabase.com → free account → new project → copy `DATABASE_URL` (transaction pooler, port 6543) and `DIRECT_DATABASE_URL` (direct connection, port 5432) from Project Settings → Database.

**Vercel (M3.19):** vercel.com → Continue with GitHub → authorize → tell Claude "Vercel is ready," don't import a repo yet.

Once both are provided: fork `umami-software/umami`, wire up `schema.prisma`, deploy, add tracking script to `Layout.astro`, verify tracking, then C2 article about the Umami setup can enter the pipeline (M3.18–M3.24).

---

## Commands Needed to Resume

```bash
git checkout main && git pull origin main
npm install
npm run build   # confirm site builds before touching anything, per CLAUDE.md session checklist
```

Local preview note: this repo is under OneDrive with `core.autocrlf=true`. If `/credits` renders empty buckets locally, that's the CRLF issue above, not a real bug, strip CRLF from the affected file for local verification only (`sed -i 's/\r$//' <file>`), git will re-normalize to LF on commit regardless.
