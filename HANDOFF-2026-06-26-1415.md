# Handoff — 2026-06-26 14:15

**Session type:** Code session — Developer Agent
**Build status:** Passes — all CI green, main is clean, all PRs merged

---

## What Was Accomplished This Session

### PR #39 — C4 publish date corrected (MERGED)
- Changed `date: 2026-07-01` → `date: 2026-06-26` in `src/content/posts/context-limits-design-constraint/index.mdx`
- Updated MILESTONES.md: C4 now shows `✅ Published | 2026-06-26` with correct title
- **Key learning documented:** Astro has no date-gated publishing. All posts in `src/content/posts/` go live on the next deploy regardless of frontmatter date. Scheduling is handled by merging the PR on the intended publish date (branch-based, Option 1).

### PR #40 — Security blockers resolved (MERGED)
- **Semgrep (Blocker #4):** `SEMGREP_APP_TOKEN` added to `rustymuffler/problme` repo secrets — reused from Celly (account-level token). CI Semgrep job is now fully operational.
- **Aikido (Blocker #3):** Installed via `/plugin install aikido@claude-plugins-official`; authenticated via `/aikido:setup`. Account-level — covers all Claude Code projects.
- BLOCKERS.md and SECURITY_SCANNING.md updated to reflect both resolutions.
- **Validation pending:** Aikido was installed this session but could not be validated (MCP tools not visible in an already-open session). A background task chip has been created — validate at the start of the next code session.

### PR #41 — Phase 2 completion (MERGED)
All four remaining Phase 2 milestones are done:

| Milestone | What was built |
|---|---|
| M2.13 | Shiki syntax highlighting — already implemented in a prior session. Milestone ticked. |
| M2.14 | `src/components/Callout.astro` — `note` (amber), `warning` (terracotta), `tip` (olive) variants. Import in MDX to use. |
| M2.15 | `src/pages/rss.xml.ts` — `/rss.xml` via `@astrojs/rss@4.0.18`. Autodiscovery `<link>` in Layout.astro. |
| M2.16 | `@astrojs/sitemap@3.7.3` in astro.config.mjs. `sitemap-index.xml` generated at build. `<link rel="sitemap">` in Layout.astro. |

Security: Trivy 0 HIGH/CRITICAL, Semgrep 0 findings, Gitleaks clean. Both rl-protect WARNs (dev dep governance gate only) acknowledged by Richard and logged in SECURITY_SCANNING.md Accepted Risk table.

---

## Current State of Main

- **Phase 1:** ✅ Complete — one item remaining: CHANGELOG.md (M1.17)
- **Phase 2:** ✅ Complete — all 19 items done
- **Phase 3:** 🔄 In progress — C4 published, scheduled tasks running, 3 more articles needed

---

## Active Blockers

| # | Blocker | Status |
|---|---|---|
| 3 | Aikido | ✅ Resolved — validate at start of next code session (background chip active) |
| 4 | Semgrep | ✅ Resolved |
| 5 | Supabase account | ⬜ Not started — blocks Umami analytics (M3.17) |
| 6 | Vercel account | ⬜ Not started — blocks Umami hosting (M3.19) |
| 7 | Umami data-website-id | 🔑 Waiting on M3.21 |

---

## Files Created or Modified This Session (all merged to main)

| File | What happened |
|---|---|
| `src/content/posts/context-limits-design-constraint/index.mdx` | Date corrected to 2026-06-26 |
| `MILESTONES.md` | C4 marked published; M2.13–M2.16 marked ✅; blockers updated |
| `BLOCKERS.md` | Blockers #3 and #4 resolved |
| `SECURITY_SCANNING.md` | Aikido marked operational; two new Accepted Risk entries |
| `src/components/Callout.astro` | New — callout MDX component (note/warning/tip) |
| `src/pages/rss.xml.ts` | New — RSS feed endpoint |
| `astro.config.mjs` | Added `@astrojs/sitemap` integration |
| `src/layouts/Layout.astro` | Added RSS autodiscovery and sitemap `<link>` tags to `<head>` |
| `package.json` / `package-lock.json` | Added `@astrojs/rss@4.0.18` and `@astrojs/sitemap@3.7.3` |

---

## Critical Invariants (Never Violate)

- **No em dashes** anywhere in article content or UI copy. Ever.
- **Interview notes are required** before any article is drafted. No exceptions.
- **Proofreader must approve** before Publisher Agent opens a PR.
- **Hero images** go in `public/assets/posts/[slug]/` and use absolute paths in frontmatter.
- **Pipeline docs** (interview notes, research briefs) go in `content-pipeline/[slug]/`, not `src/content/posts/`.
- **Astro has no date-gated publishing.** Scheduling = merge the PR on the publish date.
- **rl-protect scan before every npm install.** No exceptions.
- **Dark mode only.** Background `#15120E`.
- **Fonts:** Hanken Grotesk + JetBrains Mono only.
- **No secrets in source files.**
- **Lighthouse minimums:** Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95.

---

## Key Decisions Made This Session

1. **Scheduling = branch-based (Option 1).** Astro has no date-gated publishing. Future articles are kept on branches/draft PRs and merged on the intended publish date.
2. **Semgrep token is account-level.** The same `SEMGREP_APP_TOKEN` from Celly works across all repos — just add it to each repo's GitHub Secrets.
3. **Aikido is account-level.** Install once via `/plugin install aikido@claude-plugins-official`, authenticate once via `/aikido:setup`, active in all Claude Code sessions.
4. **Callout components are import-based.** Authors import `Callout.astro` directly in MDX files — no global component registration needed in Astro.

---

## Exact Next Steps

### Next code session — Phase 3 setup
1. Run `npm run build` to confirm build passes
2. Validate Aikido MCP is active (background chip will prompt this — use ToolSearch query "aikido")
3. Address 4 Dependabot vulnerabilities on main (1 high, 2 moderate, 1 low)
4. Write CHANGELOG.md v0.1.0 entry (M1.17 — last remaining Phase 1 item)
5. Walk through Umami analytics setup (M3.17–M3.24) — see Phase 3 checklist below

### Phase 3 — What Richard must do personally

**Step 1 — Google Search Console (M3.16)**
- Go to [search.google.com/search-console](https://search.google.com/search-console)
- Add property for `https://probl.me`
- Verify ownership (HTML tag method: Claude adds a `<meta>` tag to Layout.astro, you merge the PR)
- Once verified, submit `https://probl.me/sitemap-index.xml`

**Step 2 — Supabase (M3.17)**
- Go to [supabase.com](https://supabase.com) and create a free account
- Create a new project (name it `umami` or similar)
- After the project is ready, go to: Project Settings → Database → Connection string
- Copy both `DATABASE_URL` (Transaction pooler) and `DIRECT_DATABASE_URL` (Session pooler / direct connection)
- Hand both strings to Claude — do not commit them anywhere

**Step 3 — Vercel (M3.19)**
- Go to [vercel.com](https://vercel.com) and create a free account
- Connect your GitHub account when prompted
- Do not import any repo yet — Claude will walk you through the rest once the Umami fork is ready

**Step 4 — Merge PRs as they come**
- Claude will open draft PRs for each article
- Review the article in the PR, then merge to publish

### Phase 3 — What Claude does once you provide credentials

Once you have the Supabase connection strings and Vercel account:
1. Fork `umami-software/umami` to `rustymuffler/umami` (M3.18)
2. Modify `schema.prisma` to add `DIRECT_DATABASE_URL` (M3.18)
3. Guide you through importing the fork into Vercel and setting env vars (M3.20)
4. Guide you through deploying Umami and adding probl.me as a tracked site (M3.21)
5. Add the Umami tracking script to `Layout.astro` (M3.22) — PR for you to merge
6. Verify analytics tracking works (M3.23)
7. Draft the article about the Umami setup (M3.24) — through the full content pipeline

### Next content session
- C1 is next: *"Writing a spec your AI agent can actually follow"* (pm-craft)
- Run Interview Agent first — no drafting without interview notes
- Full pipeline: Interview → Research → Writer → SEO Review → Proofread → Image → Publisher
