# Handoff — 2026-06-26 02:15

**Session type:** Mixed — PM setup + Content pipeline execution
**Build status:** Passes — all CI green, PR #37 merged to main

---

## What Was Accomplished This Session

### Thread B — Content Pipeline Setup (COMPLETE)

**Scheduled tasks created (all 4):**
| Task ID | Schedule | Purpose |
|---|---|---|
| `probme-monday-idea-batch` | Every Monday 9am | Content Strategist Agent generates 5–10 ideas |
| `probme-friday-queue-check` | Every Friday 9am | PM Agent checks draft PR count, alerts if < 4 |
| `probme-pr-reminder` | Every Tue/Thu 8am | PM Agent lists open draft PRs for Richard |
| `probme-monthly-retro` | First Monday of month 10am | PM Agent runs monthly retrospective; next run 2026-07-06 |

**Content calendar established:**
- First idea batch run manually this session
- Richard selected 7 articles (C1–C7) across pm-craft, ai-development, and tech-tools
- All 7 logged in MILESTONES.md content calendar

**retros/ folder created and committed.**

---

### Article C4 — Full Pipeline Run (COMPLETE, MERGED)

Article: *"How Context Limits Changed the Way I Build with AI"*
Category: ai-development | Slug: `context-limits-design-constraint` | Publish date: 2026-07-01

**Pipeline stages completed:**
- Interview Agent — 6 questions + follow-ups with Richard; notes saved to `content-pipeline/context-limits-design-constraint/interview-notes.md`
- Research Agent — research brief with verified stats, competitive scan, credible sources, counterarguments; saved to `content-pipeline/context-limits-design-constraint/research-brief.md`
- Writer Agent — ~1,320 word MDX draft in Richard's voice; all 6 interview themes present
- SEO Reviewer — returned two issues (title too long, word count under minimum); both fixed inline
- Proofreader — returned 6 line-level issues on first pass (passive voice, long sentences, consecutive same-word starts); all fixed; approved on second pass
- Image Creator Agent — ran in Claude Cowork (Claude Design); hero image produced and committed
- Publisher Agent — PR #37 opened as draft, all CI fixed, merged to main

**CI fixes required before merge:**
1. `interview-notes.md` and `research-brief.md` were inside `src/content/posts/` — Astro validated them against the posts schema and failed. Fixed by moving to `content-pipeline/[slug]/`.
2. `heroImage: ./images/hero.png` — relative path not processed by Astro's image pipeline (schema is `z.string()`, not `image()`). Fixed by moving image to `public/assets/posts/[slug]/hero.png` and using absolute path in frontmatter.
3. `.lighthouseci/` local artifacts accidentally committed; removed and added to `.gitignore`.

**PR #37 merged. Branch deleted. Article deploys to probl.me on 2026-07-01.**

---

## Files Created or Modified This Session

| File | What happened |
|---|---|
| `MILESTONES.md` | Marked 1.14, 1.15, 2.19, 3.1–3.6 ✅; added Content Calendar section with C1–C7 |
| `retros/.gitkeep` | Created; retros/ directory now tracked |
| `content-pipeline/context-limits-design-constraint/interview-notes.md` | Interview notes from Richard (6 questions + follow-ups) |
| `content-pipeline/context-limits-design-constraint/research-brief.md` | Research brief with verified sources |
| `src/content/posts/context-limits-design-constraint/index.mdx` | Published article (merged to main via PR #37) |
| `public/assets/posts/context-limits-design-constraint/hero.png` | Hero image (Claude Design, 1200×630px) |
| `.gitignore` | Added `.lighthouseci/` |

**Scheduled tasks created (in Claude scheduled tasks sidebar):**
- `probme-monday-idea-batch`
- `probme-friday-queue-check`
- `probme-pr-reminder`
- `probme-monthly-retro`

---

## Content Calendar — Current State

| # | Working Title | Category | Status |
|---|---|---|---|
| C1 | Writing a spec your AI agent can actually follow | pm-craft | ⬜ Queued |
| C2 | How I built a fully automated content pipeline with Claude agents | ai-development | ⬜ Queued |
| C3 | Multi-agent workflows without an orchestration framework: the AGENTS.md approach | ai-development | ⬜ Queued |
| C4 | How Context Limits Changed the Way I Build with AI | ai-development | ✅ Published (2026-07-01) |
| C5 | Scheduled Claude agents: setting up autonomous loops that don't need babysitting | ai-development | ⬜ Queued |
| C6 | Astro on GitHub Pages in 2026: what the docs don't tell you | tech-tools | ⬜ Queued |
| C7 | Why I SHA-pin every GitHub Action and how I automate keeping them current | tech-tools | ⬜ Queued |

---

## Active Blockers (unchanged)

| # | Blocker | Status |
|---|---|---|
| 3 | Aikido API key | ⬜ Not started |
| 4 | Semgrep App Token | ⬜ Not started |
| 5–7 | Supabase / Vercel / Umami | ⬜ Not started |

**Dependabot:** 4 vulnerabilities on main (1 high, 2 moderate, 1 low) — flagged as a background task chip this session. Worth addressing before next code session.

---

## Decisions Made This Session

1. **Pipeline docs live in `content-pipeline/[slug]/`** — not inside `src/content/posts/`. Astro validates all files in the posts collection against the schema. Interview notes and research briefs have no post frontmatter, so they must live outside the collection.

2. **Hero images live in `public/assets/posts/[slug]/`** — `heroImage` in the content schema is `z.string()`, not Astro's `image()` helper, so relative paths don't get processed. All future article hero images use `public/assets/posts/[slug]/hero.png` and are referenced with an absolute `/assets/posts/[slug]/hero.png` path in frontmatter.

3. **`.lighthouseci/` added to `.gitignore`** — local Lighthouse CI run artifacts should never be committed.

4. **Draft PRs** are opened by the Publisher Agent and must be manually converted to "Ready for review" by Richard before merging.

---

## Exact Next Steps

### When starting the next content session:

1. Read this handoff + CLAUDE.md + AGENTS.md
2. Pick the next article from the content calendar — C1 is the natural next choice (pm-craft category needs representation)
3. Run the Interview Agent for C1: *"Writing a spec your AI agent can actually follow"*
4. Follow the full pipeline: Interview → Research → Writer → SEO Review → Proofread → Image (Cowork) → Publisher

### When starting the next code session (Thread A — deferred):

1. Run `npm run build` to confirm build passes before touching anything
2. Items remaining:
   - Run rl-protect scan on `@astrojs/rss` and `@astrojs/sitemap`, then install
   - Mark MILESTONES.md 2.13 ✅ (Shiki already implemented via CodeBlock.astro)
   - Write CHANGELOG.md for v0.1.0
   - Build MDX callout components (note/warning/tip) — 2.14
   - Address Dependabot vulnerabilities (4 open: 1 high, 2 moderate, 1 low)

---

## Critical Invariants (Never Violate)

- **No em dashes** anywhere in article content or UI copy. Ever.
- **Interview notes are required** before any article is drafted. No exceptions.
- **Proofreader must approve** before Publisher Agent opens a PR.
- **Hero images** go in `public/assets/posts/[slug]/` and use absolute paths in frontmatter.
- **Pipeline docs** (interview notes, research briefs) go in `content-pipeline/[slug]/`, not `src/content/posts/`.
- **Draft PRs** require "Ready for review" click before merge.
- **Dark mode only.** Background `#15120E`. No light mode.
- **Fonts:** Hanken Grotesk + JetBrains Mono only. No third font without updating BRAND.md.
- **No secrets in source files.** Use environment variables and GitHub Secrets.
- **Lighthouse minimums:** Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95.
